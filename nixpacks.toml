[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["bundle install", "npm install"]

[phases.build]
cmds = ["SECRET_KEY_BASE_DUMMY=1 RAILS_ENV=production bundle exec rails assets:precompile"]