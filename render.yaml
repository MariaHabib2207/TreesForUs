services:
  - type: web
    name: treesofus
    env: ruby
    buildCommand: "bundle install && bundle exec rails assets:precompile && bundle exec rails db:migrate"
    startCommand: "bundle exec rails db:seed && bundle exec rails server -b 0.0.0.0"undle exec rails server -b 0.0.0.0